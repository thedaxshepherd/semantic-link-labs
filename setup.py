from setuptools import setup, find_packages
setup(
    	name='jr_Labs', #needs to build fabric
    	version='1.0.0',
    	description='A sample library of utilities to use in Fabric notebooks',
   	 packages=find_packages()
)
